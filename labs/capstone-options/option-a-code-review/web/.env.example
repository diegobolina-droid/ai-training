# Local dev: leave unset — Vite proxies /review and /health to the API (see vite.config.ts).

# Production / Vercel: set in Project → Settings → Environment Variables (Production + Preview if needed).
# No trailing slash. Required whenever the UI and API are on different hosts.
# VITE_API_BASE=https://your-api-deployment.vercel.app
